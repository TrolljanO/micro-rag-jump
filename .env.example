# Exemplo de arquivo de variáveis de ambiente
# NÃO inclua chaves reais aqui. Use este arquivo como modelo e crie um
# arquivo `.env` local a partir dele (por exemplo: `cp .env.example .env`).

# LLM / API
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
OPENAI_API_BASE_URL=https://api.example.com
MODEL_NAME=openai/gpt-4.1-nano
EMBEDDING_MODEL=openai/text-embedding-3-small

# Embeddings (compatibilidade / fallback)
EMBEDDING_MODEL=text-embedding-3-small

# Configurações RAG
CHUNK_SIZE=800
CHUNK_OVERLAP=100
TOP_K=3

# App
DEBUG=True
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=8000
# CORS (adicionar URL de produção depois do deploy)
CORS_ORIGINS=http://localhost:5173,https://seu-app.vercel.app

# Notas:
# - Substitua valores como `YOUR_OPENAI_API_KEY` pelas chaves reais no seu
#   arquivo `.env` local (não commitar).
# - Considere usar variáveis de ambiente do sistema ou um secret manager
#   em produção em vez de arquivos .env.